pipeline {
    agent any
    stages {
        stage('Install Dependencies') {
            steps {
                echo 'Installing backend and frontend dependencies...'
                // Example: sh 'cd backend && npm install'
            }
        }
        stage('Build') {
            steps {
                echo 'Building Nidar Project...'
            }
        }
        stage('Test') {
            steps {
                echo 'Running Scout Drone simulations...'
            }
        }
    }
}
